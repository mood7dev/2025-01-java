package ch04.sec03;

public class SwitchStudy {
    public static void main(String[] args){
/*
    switch 문에서는 관계 연산자(>, <, >=, <=, !=)를 사용할 수 없다.
    오직 '==' (동등 비교)만 가능하며, 그마저도 내부적으로는 정수형 또는 열거형 값의 일치 여부를 기준으로 작동한다.

    switch 문은 case에서 일치하는 값을 찾으면 해당 case로 진입하여,
    break 키워드를 만날 때까지 이후 모든 case 문을 실행한다.

    기본 구조는 다음과 같다:

    switch (변수) {
        case 값1:
            // 처리1
            break;
        case 값2:
            // 처리2
            break;
        ...
        default:
            // 모든 case와 일치하지 않을 때 처리
            break;
    }
*/
    }
}
